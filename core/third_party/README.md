# Third-party sources

Vendored C sources land here as git submodules, one per library, each carrying its
own `LICENSE` file — starting with libarchive, 7-Zip and the libyal libraries in M3
(see `docs/agent/MASTER_PLAN.md` section 4.2). Empty until then.
