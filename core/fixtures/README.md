# Format fixtures

One valid and one hostile file per format, generated by scripts in `tools/fixtures/`
per `docs/agent/MASTER_PLAN.md` section 3.4. No file over 2 MB is committed; larger
cases are generated at test time. Empty until the first format-parsing crate lands.
