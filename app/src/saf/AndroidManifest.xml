<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Play-safe flavor. Intentionally empty of storage permissions: this build reaches files
        only through Storage Access Framework grants the user makes explicitly, which is
        README principle 2 and docs/ARCHITECTURE.md's default path.

        Do not add MANAGE_EXTERNAL_STORAGE, READ_MEDIA_* or legacy READ/WRITE_EXTERNAL_STORAGE
        here. Broad access belongs in src/full only.
    -->

</manifest>
