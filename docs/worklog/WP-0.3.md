# WP-0.3 — Merge PR #13 (docs)

**Status: already DONE before this session.**

`main`'s tip at session start is `326346a docs: describe what Fylz is and how it is built (#13)`
— PR #13 was merged with a merge-commit-shaped history (the branch commit `37539f3` is its
parent) before this work began. Nothing to do.

Noted for the record: the repo convention stands — merge commits, never squash, while submodule
pins are in play (a squash of `shared-libraries` would orphan Fylz's `7a6ff2d` pin).
